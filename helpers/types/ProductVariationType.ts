type ProductVariationType = {
    id: number,
    sku: string,
    points: number,
    price: number,
    new_price: number,
    available_quantity: number,
    in_stock: number,
    branch_id: number,
    user_group_percentage_value: number
}

export default ProductVariationType